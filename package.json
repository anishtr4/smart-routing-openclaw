{
  "name": "@local/smart-llm-router",
  "version": "1.0.0",
  "description": "Smart LLM router for OpenClaw - automatically routes to the most cost-effective model",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test-router": "npm run build && npx tsx test-router.ts",
    "install-plugin": "npm run build && npm link && echo '\n✅ Plugin built and linked! Now run:\n   openclaw plugin install $(pwd)\n'",
    "uninstall-plugin": "openclaw plugin uninstall @local/smart-llm-router && npm unlink",
    "reinstall": "npm run uninstall-plugin ; npm run install-plugin"
  },
  "keywords": [
    "openclaw",
    "llm",
    "router",
    "ai",
    "cost-optimization"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@google/generative-ai": "^0.21.0",
    "groq-sdk": "^0.7.0",
    "openai": "^4.67.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "openclaw": {
    "extensions": [
      {
        "type": "skill",
        "id": "smart-router",
        "name": "Smart Router",
        "description": "Intelligent cost-optimized routing across multiple LLM providers",
        "entry": "dist/index.js",
        "schema": {
          "type": "object",
          "properties": {
            "anthropicApiKey": {
              "type": "string",
              "description": "Anthropic API key (or set ANTHROPIC_API_KEY env var)",
              "default": ""
            },
            "googleApiKey": {
              "type": "string",
              "description": "Google AI API key (or set GOOGLE_API_KEY env var)",
              "default": ""
            },
            "groqApiKey": {
              "type": "string",
              "description": "Groq API key (or set GROQ_API_KEY env var)",
              "default": ""
            },
            "openaiApiKey": {
              "type": "string",
              "description": "OpenAI API key (or set OPENAI_API_KEY env var)",
              "default": ""
            },
            "defaultTier": {
              "type": "string",
              "enum": [
                "simple",
                "medium",
                "complex",
                "reasoning"
              ],
              "description": "Fallback tier when routing confidence is low",
              "default": "medium"
            },
            "enableLogging": {
              "type": "boolean",
              "description": "Log routing decisions and costs",
              "default": true
            },
            "costTracking": {
              "type": "boolean",
              "description": "Track and display cumulative costs",
              "default": true
            }
          }
        }
      }
    ]
  }
}